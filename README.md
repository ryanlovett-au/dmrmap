# dmrmap
A tool for auto generating a map of VK (Australian) DMR Repeaters

To use, just deploy the site/folder to any static hosting (Cloudflare Pages, Netlify, etc.) and run the PHP tool periodically to regenerate the GeoJSON.
